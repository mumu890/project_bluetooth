<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景图 -->
    <item android:id="@android:id/background" android:color="@color/black_overlay" />
    <!-- 进度度 -->
    <item android:id="@android:id/progress" android:color="@color/colorAccent" />
</layer-list>